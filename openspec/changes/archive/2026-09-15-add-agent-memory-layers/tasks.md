## 1. Модель памяти

- [x] 1.1 Добавить `memory.rs` с типами `Profile`, `Task`, `TaskPhase` и активной памяти, подключить модуль через тонкий `main.rs` и проверить unit-тестами допустимые значения и отображение фаз.
- [x] 1.2 Реализовать детерминированный переход `planning -> execution -> validation -> done` без пропусков и проверить тестами каждый переход и ошибку перехода из `done`.
- [x] 1.3 Добавить валидацию непустых имён, инструкций и TODO с ограничениями длины и проверить граничные и ошибочные значения unit-тестами.

## 2. Хранение и миграции SQLite

- [x] 2.1 Создать миграцию таблицы `profiles` и nullable-поля `sessions.profile_id`, затем проверить на временной БД создание, выбор, сохранение связи и загрузку legacy-сессии без профиля.
- [x] 2.2 Реализовать операции создания, списка и загрузки профилей и проверить round-trip и уникальность имени профиля тестами `SessionStore`.
- [x] 2.3 Создать миграцию таблицы `tasks` и nullable-поля `sessions.task_id`, затем проверить на временной БД создание задачи в `planning`, сохранение связи и загрузку legacy-сессии без задачи.
- [x] 2.4 Реализовать операции списка и загрузки задач, замены TODO и подтверждённого перехода фазы в транзакции; проверить сохранение между повторными открытиями БД и отказ от недопустимых переходов.
- [x] 2.5 Расширить `SavedSession` и `SessionSnapshot` ссылками на профиль и задачу и проверить, что обновление существующей сессии не теряет историю, summary, facts, ветки или checkpoint.

## 3. Формирование контекста агента

- [x] 3.1 Передавать активный профиль и задачу в настройки агента, не связывая `Agent` с SQLite, и проверить восстановление и очистку активной памяти тестами `AgentPool`.
- [x] 3.2 Собрать типизированные секции системного prompt в порядке «режим, профиль, рабочая память, сессионный контекст» и проверить точный состав для запросов со всеми слоями и без каждого необязательного слоя.
- [x] 3.3 Добавить фазовые инструкции для `planning`, `execution`, `validation` и `done`, включая запрет самостоятельного перехода, и проверить их unit-тестом для каждой фазы.
- [x] 3.4 Проверить тестами, что профиль и задача попадают в запрос при всех стратегиях сжатия, а обычное сообщение, summary и sticky facts не изменяют записи профиля, TODO или фазу.

## 4. Команды профиля и задачи

- [x] 4.1 Реализовать `/profile` с просмотром, созданием, выбором и отключением профиля; проверить CLI-тестами подсказку команды, отмену ввода и изменение активного профиля только после подтверждения.
- [x] 4.2 Реализовать `/task` с просмотром, созданием и продолжением задачи, а также редактированием TODO; проверить CLI-тестами создание в `planning`, отмену и явное сохранение нового TODO.
- [x] 4.3 Реализовать `/task next` с отображением исходной и целевой фаз и подтверждением пользователя; проверить, что отказ не пишет в БД, подтверждение делает ровно один переход, а `done` не имеет следующей фазы.
- [x] 4.4 При смене профиля или задачи после появления сообщений сохранять старую и начинать новую сессию; проверить интеграционными тестами, что каждая сессия имеет не более одного `profile_id` и одного `task_id`.
- [x] 4.5 Реализовать read-only команду `/memory` и компактное отображение профиля и задачи в статус-баре; проверить вывод для пустой и полностью заполненной памяти без отображения секретов конфигурации.
- [x] 4.6 Показывать над статус-баром строку текущей фазы с отображением `planning` синим, `execution` жёлтым, `validation` пурпурным и `done` зелёным; проверить текст, ANSI-цвет каждой фазы, обновление после перехода и восстановления, очистку служебных строк и отсутствие индикатора без активной задачи.

## 5. Восстановление и сквозное поведение

- [x] 5.1 При продолжении сессии восстанавливать связанный профиль и актуальное состояние задачи и проверить сквозным тестом повторное открытие БД и продолжение одной задачи из нескольких сессий.
- [x] 5.2 Проверить сквозным тестом `/new`, что история предыдущего диалога не используется, а профили и незавершённые задачи остаются доступными для явного выбора.
- [x] 5.3 Обновить `/help`, автодополнение и русскоязычные сообщения об ошибках и проверить существующие и новые команды тестами completion/parser.

## 6. Документация и проверка результата

- [x] 6.1 Обновить `README.md` описанием трёх слоёв, команд `/profile`, `/task`, `/memory`, цветного индикатора фаз, правил начала новой сессии и pipeline; актуализировать `AGENTS.md` с новым `memory.rs`, командами и относящимися к памяти правилами тестирования и проверить, что оба документа соответствуют фактическому проекту.
- [x] 6.2 Создать `reports/day11/report.md`, провести воспроизводимый сценарий без памяти, с профилем, с задачей на каждой фазе и после `/new`, затем зафиксировать, какие данные попали в каждый слой и как изменились ответы агента. **Пропущено по решению пользователя:** фактические платные API-вызовы пользователь выполнит самостоятельно; в отчёте сохранён воспроизводимый сценарий без вымышленных результатов.
- [x] 6.3 Запустить `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings` и `cargo test` и записать успешные команды проверки в отчёт Дня 11.

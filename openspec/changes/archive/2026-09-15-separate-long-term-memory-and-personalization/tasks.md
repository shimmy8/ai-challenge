## 1. Модель и хранение долговременной памяти

- [x] 1.1 Добавить в `memory.rs` тип `MemoryEntry` для долговременного факта и ограничение длины содержимого; проверить unit-тестами trim, пустой ввод и границу Unicode-длины.
- [x] 1.2 Создать в `SessionStore::open` таблицу `memory_entries` без классификации и CRUD-операции создания, списка, загрузки и удаления; проверить на временной SQLite-базе round-trip нескольких фактов и сохранность после повторного открытия.
- [x] 1.3 Проверить миграцию существующей базы без `memory_entries`: открытие создаёт пустую долговременную память и не изменяет профили, задачи, историю, summary, facts, ветки и ссылки сессий.

## 2. Подключение памяти к контексту агента

- [x] 2.1 Расширить `ActiveMemory` коллекцией долговременных записей и загружать актуальный снимок при запуске, `/new`, восстановлении сессии, `/remember` и `/forget`; проверить, что `AgentPool` передаёт одинаковый снимок всем агентам и не очищает его вместе с краткосрочной сессией.
- [x] 2.2 Добавить в `Agent::request_settings` отдельную секцию долговременных фактов после рабочей памяти и до производного сессионного контекста; проверить точный порядок секций, наличие `id`, маркировку как данных и применение при `summary`, `sliding-window`, `sticky-facts` и `branching`.
- [x] 2.3 Добавить регрессионный тест, что `/remember` изменяет только коллекцию долговременных фактов, а обычные сообщения, история, summary, sticky facts, переключение веток, активная задача, TODO и фаза остаются без изменений.

## 3. Явные команды долговременной памяти

- [x] 3.1 Реализовать интерактивную `/remember` и детерминированную форму `/remember <текст>` с фиксированной семантикой долговременного факта; проверить parser, многострочный ввод, пустой текст и отмену без записи в БД.
- [x] 3.2 Реализовать `/forget <id>` с показом целевой записи и обязательным подтверждением; проверить сохранение записи при отказе, удаление ровно выбранной записи при согласии и ошибку неизвестного id.
- [x] 3.3 Обновить `/help`, автодополнение и русскоязычные сообщения для `/remember` и `/forget`; проверить доступность подсказок и раскрытие сокращённых команд тестами CLI.
- [x] 3.4 Обновить `/memory`, чтобы дополнительно показывать сохранённые долговременные факты и их id; проверить пустой и заполненный вывод и отсутствие секретов конфигурации, не меняя существующее отображение сессии, задачи и профиля.

## 4. Профиль персонализации

- [x] 4.1 Изменить интерактивное создание профиля: отдельно запрашивать стиль, формат и ограничения и собирать канонический текст инструкций чистой функцией; проверить заголовки, порядок, trim, ограничения длины и отмену без сохранения.
- [x] 4.2 Переименовать секцию prompt и пользовательские подписи с «долговременного профиля» на «профиль персонализации», сохранив таблицу `profiles`, поле `instructions`, команды и `profile_id`; проверить загрузку и применение профиля прежнего свободного формата без изменения текста.
- [x] 4.3 Проверить регрессионными тестами создание, выбор, отключение, восстановление профиля и начало новой сессии при его смене, а также отсутствие автоматического изменения профиля обычными сообщениями или долговременными записями.

## 5. Проверка каждого provider payload

- [x] 5.1 Выделить чистые функции построения JSON payload OpenAI и Claude без изменения HTTP-поведения; проверить существующие обязательные поля, условный `reasoning`, температуру и отсутствие системного поля без инструкций.
- [x] 5.2 Проверить, что активный профиль попадает в `instructions` OpenAI и `system` Claude при каждом повторном основном запросе и при всех стратегиях сжатия, а долговременные факты остаются внутри маркированной секции данных.
- [x] 5.3 Создать два контрастных профиля и автоматическим тестом подтвердить разные итоговые системные инструкции для одного контрольного запроса без дублирования предпочтений в тексте запроса.

## 6. Документация и итоговая валидация

- [x] 6.1 Дополнить `README.md` описанием новых команд `/remember` и `/forget` и их изоляции от сессии и задачи; проверить соответствие документации фактическому CLI, не изменяя архив, specs или отчёты Дня 11.
- [x] 6.2 Создать `reports/day12/report.md` с текстами двух контрастных профилей, одинаковым контрольным запросом и критериями сравнения стиля, формата и ограничений; при выполненном ручном эксперименте приложить фактические ответы, иначе оставить воспроизводимые команды и незаполненные места результатов.
- [x] 6.3 Запустить `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test` и `openspec validate separate-long-term-memory-and-personalization --strict`; записать успешные команды и устранить все найденные ошибки.

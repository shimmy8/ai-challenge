## 1. Версия плана и хранение

- [x] 1.1 Добавить в `Task` текущую и утверждённую версии плана и отобразить их при загрузке; проверить тестом, что новая задача имеет версию `0` без утверждения.
- [x] 1.2 Создать поля версий в новой базе и добавлять только недостающие столбцы в существующей; проверить тестами новую задачу в прежней базе, повторное открытие без повторной миграции и сохранение фазы и TODO старой задачи без присвоения ей утверждения.
- [x] 1.3 Увеличивать версию один раз при фактическом успешном изменении TODO в `planning`, сохраняя её при пустом, повторном или отклонённом обновлении; проверить тестами обе ветви и откат при ошибке записи.

## 2. Правила и переходы

- [x] 2.1 Вынести фазовые условия готовности и причины отказа в доменную логику; проверить тестами пустой план, пустые рабочие секции, незавершённое выполнение, незавершённую валидацию и терминальный `done`.
- [x] 2.2 Сделать `SessionStore::advance_task` проверяющим ожидаемые фазу и версию, готовность и утверждение, с атомарной записью утверждённой версии при `planning -> execution`; проверить тестами успешную цепочку и неизменность состояния после каждого отказа.
- [x] 2.3 Проверить устаревшее подтверждение: изменить план после его показа и до вызова перехода; тест должен подтвердить отказ без изменения фазы и утверждения, а повторный вызов с новой версией должен проходить.

## 3. CLI, агент и продолжение

- [x] 3.1 Показывать при `/task next` план и его версию перед утверждением, а при недопустимом переходе — конкретную причину без запроса подтверждения; проверить выводом CLI или узконаправленными тестами форматирования и команды.
- [x] 3.2 Передавать текущую и утверждённую версии в контекст активной задачи, сохраняя фазовые ограничения `TASK_UPDATE`; проверить тестами prompt для `planning`, `execution`, `validation` и отклонение обновления чужой фазы.
- [x] 3.3 Проверить отмену подтверждения и восстановление после паузы: повторное открытие базы и сессии должно сохранять версию, утверждение, фазу, TODO и следующий незавершённый пункт; проверить отдельным регрессионным тестом.
- [x] 3.4 Распознавать `TASK_UPDATE` после текста на той же последней строке ответа; сохранить видимый текст, проверить регрессионным тестом непустое и пустое обновление.
- [x] 3.5 При некорректном JSON `TASK_UPDATE` показывать фрагмент полученного текста и сообщать, что TODO не изменён; проверить регрессионным тестом.

## 4. Общая проверка

- [x] 4.1 Запустить `cargo fmt --all`, `cargo clippy --all-targets --all-features -- -D warnings` и `cargo test`; проверить отсутствие ошибок и предупреждений и зафиксировать результат проверки.
- [x] 4.2 Создать `reports/day15/report.md`: описать модель состояний, версию и утверждение плана, результаты автоматических проверок и пошаговый ручной пример на новой задаче в текущей базе (отказ без плана, утверждение версии, отказ при незавершённом выполнении, продолжение после паузы, отказ завершить до проверки, успешный переход в `done`); проверить наличие ожидаемого результата у каждого шага и оставить раздел фактических наблюдений пользователя незаполненным до его прогона.

## ADDED Requirements

### Requirement: Calendar events can be read for an absolute interval
CalDAV-интеграция SHALL запрашивать события выбранного Яндекс Календаря, пересекающие заданный полуоткрытый интервал `[from, to)`, где обе границы являются абсолютными RFC 3339 значениями со смещением и `to` позже `from`. Результат SHALL нормализовать название, начало, окончание, признак события на весь день и необязательное место, упорядочив события по началу.

#### Scenario: Timed events overlap the requested interval
- **WHEN** CalDAV возвращает одно или несколько событий, пересекающих валидный интервал
- **THEN** система возвращает нормализованные события в хронологическом порядке

#### Scenario: All-day event is returned
- **WHEN** CalDAV возвращает событие с датой без времени
- **THEN** результат сохраняет его как событие на весь день и не придумывает локальное время начала

#### Scenario: Requested interval is invalid
- **WHEN** граница не содержит смещения либо окончание не позже начала
- **THEN** система отклоняет запрос до обращения к CalDAV

#### Scenario: Calendar contains no matching events
- **WHEN** CalDAV успешно отвечает, но в интервале нет событий
- **THEN** система возвращает успешный пустой список

### Requirement: Digest reads use the explicitly selected calendar
Чтение событий SHALL применять тот же безопасный выбор календаря, что и запись: использовать единственный доступный календарь либо однозначное совпадение с локально настроенным именем. При нескольких доступных календарях без настройки система MUST завершить чтение ошибкой и MUST NOT объединять личные данные из произвольных календарей.

#### Scenario: One calendar is available
- **WHEN** CalDAV discovery возвращает ровно один доступный календарь
- **THEN** события читаются из него без дополнительной настройки

#### Scenario: Several calendars are ambiguous
- **WHEN** доступно несколько календарей и целевое имя не настроено
- **THEN** чтение не выполняется, а результат содержит безопасную инструкцию выбрать календарь

### Requirement: Calendar read results and errors do not expose credentials
Запрос и результат чтения MUST NOT включать CalDAV-логин, пароль приложения, заголовок авторизации или необработанное содержимое ответа, способное раскрыть реквизиты. Некорректные отдельные события SHALL быть пропущены с безопасной диагностикой, а системная ошибка ответа SHALL завершать сбор без ложного пустого результата.

#### Scenario: One event is malformed
- **WHEN** ответ содержит корректные события и отдельный элемент, который невозможно нормализовать
- **THEN** система возвращает корректные события и фиксирует безопасное предупреждение без сырого приватного содержимого

#### Scenario: CalDAV authorization fails
- **WHEN** сервер отклоняет реквизиты чтения
- **THEN** сбор завершается безопасной ошибкой настройки без значений реквизитов и без представления результата как пустого календаря

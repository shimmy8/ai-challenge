## 1. Хранение и команды

- [x] 1.1 Добавить тип и валидацию глобального инварианта с ограничениями длины и числа правил; проверить unit-тестами пустой текст, границы, идентификаторы и отсутствие неявного изменения из обычного сообщения.
- [x] 1.2 Добавить отдельную SQLite-таблицу и операции создания, списка и удаления инвариантов; проверить на временной базе сохранение после повторного открытия и миграцию legacy-базы без этой таблицы.
- [x] 1.3 Добавить `/invariant add`, `/invariant list` и подтверждаемую `/invariant remove`, обновить `/help`; проверить обработку корректных и ошибочных команд, отмену удаления и отсутствие изменений сессии, задачи, профиля и фактов.
- [x] 1.4 Загружать глобальный набор при старте и обновлять его у всех агентов только после успешной записи в БД; проверить, что `/new`, восстановление сессии и смена ветки сохраняют актуальные правила.
- [x] 1.5 Добавить сохраняемое состояние `enabled` с миграцией существующей таблицы, включением по умолчанию и операцией переключения по ID; проверить сохранение состояния, повторное включение и неизменность ID и текста.
- [x] 1.6 Сделать `/invariant` интерактивным меню по образцу `/task` с просмотром, добавлением, включением, выключением и удалением; сохранить прямые команды, показать состояние в списке и проверить отмену выбора и ошибочный ID.
- [x] 1.7 Передавать агентам только включённые правила после переключения и загрузки; проверить отсутствие выключенных правил в обоих payload и пропуск проверки, когда все правила выключены.

## 2. Применение правил в запросах

- [x] 2.1 Добавить приоритетную секцию инвариантов с ID и текстом в системные инструкции каждого основного запроса, не помещая их в историю, summary или sticky facts; проверить порядок секций и сохранение правил при всех стратегиях сжатия.
- [x] 2.2 Проверить payload обоих провайдеров с инвариантами и без них, а также при конфликтующем профиле; подтвердить тестами наличие системной секции и приоритет правила.

## 3. Отдельная проверка черновика

- [x] 3.1 Реализовать отдельный вызов проверяющей модели и строгий разбор JSON с исходами `allow`, `compliant_refusal`, `violation`, `uncertain`, ID и причиной; проверить типы, обязательность ID, неизвестный ID, лишние и несогласованные поля, а также все четыре исхода без реального API.
- [x] 3.2 Подключить проверку полного сырого ответа, включая `TASK_UPDATE`, до разбора обновления, записи истории и передачи результата в `app.rs`; проверить, что разрешённый ответ проходит обычный путь, а непроверенный черновик не попадает в историю.
- [x] 3.3 При `violation` формировать по фиксированному шаблону отказ с ID и сохранённым текстом правила, не раскрывая черновик и свободную причину проверяющей модели; проверить сохранение только шаблонного отказа, конфликтующий запрос и нарушение в ответе на допустимый запрос, включая отсутствие обновления TODO.
- [x] 3.4 При ошибке API, невалидном вердикте или `uncertain` останавливать выдачу и сохранение ответа; проверить восстановление всего состояния агента до запроса (история, summary, sticky facts, ветки, checkpoint), отсутствие изменений задачи и отдельные сообщения о техническом сбое и неопределённости.
- [x] 3.5 Проверить, что `compliant_refusal` принимается без `TASK_UPDATE`, а сочетание с `TASK_UPDATE` отклоняется с восстановлением состояния агента до запроса.
- [x] 3.6 Учитывать токены и время проверяющего вызова, включая фактически потраченные токены при сбое проверки, и пропускать его при пустом наборе; проверить метрики и отсутствие дополнительного запроса без инвариантов.

## 4. Сквозная проверка и документация

- [x] 4.1 Проверить на подменяемых ответах OpenAI и Claude совместимый запрос, конфликт с ограничением стека, проверенный отказ, запрещённый `TASK_UPDATE`, автоматический шаг задачи и сбой проверки; убедиться, что фаза не меняется автоматически и следующий шаг не запускается после отказа или ошибки.
- [x] 4.2 Обновить README с командами, областью действия, примером объяснения отказа и ограничением модельной проверки; проверить соответствие описания фактическому CLI.
- [x] 4.3 Сохранить в `reports/day14/` эксперимент с конфликтующим и совместимым запросами, ответом ассистента и результатом проверки без API-ключей и личных диалогов; проверить, что отчёт различает отказ по правилу и технический сбой.
- [x] 4.4 Выполнить `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test` и `openspec validate add-global-invariants --strict`; все проверки должны завершиться успешно.
- [x] 4.5 Обновить README и отчёт дня 14 для меню и включения/выключения, затем повторить форматирование, Clippy, тесты и строгую проверку OpenSpec.

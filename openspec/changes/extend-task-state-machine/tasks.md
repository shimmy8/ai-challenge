## 1. Структурированная модель TODO

- [x] 1.1 Добавить типы `TaskTodo` и `TodoItem` с фактами, execution/validation-списками, стабильными ID и флагами завершения; проверить unit-тестами равенство, порядок и выделение новых ID.
- [x] 1.2 Добавить ограничения числа и Unicode-длины фактов и пунктов, нормализацию пробелов и дедупликацию; проверить тестами пустые значения, границы и повторные добавления.
- [x] 1.3 Обновить фазовые инструкции: planning формирует факты и оба списка, execution завершает execution-пункты, validation завершает validation-пункты, done не меняет TODO; проверить unit-тестами тексты всех четырёх фаз и запрет автоматической смены фазы.

## 2. Компактное TOON-кодирование и хранение

- [x] 2.1 Реализовать канонический TOON encoder для секций `f`, `e` и `v` с табличными полями `{id,x,text}`; проверить snapshot-тестом компактный формат без повторяющихся ключей.
- [x] 2.2 Реализовать строгий decoder с JSON-экранированием строк и проверкой count, ID и флагов; проверить round-trip для Unicode, запятых, кавычек и многострочного текста, а также ошибки повреждённого ввода.
- [x] 2.3 Добавить legacy fallback, преобразующий прежний текстовый TODO в один факт и пустые списки без изменения фазы; проверить миграционным тестом чтение старой базы и каноническую запись после первого обновления.
- [x] 2.4 Перевести создание, загрузку, список и обновление задач на типизированный TODO в существующем столбце `tasks.todo`; проверить сохранение полного состояния после закрытия и повторного открытия SQLite.

## 3. Автоматический delta прогресса

- [x] 3.1 Добавить тип и parser последней строки `TASK_UPDATE:` с короткими ключами `f`, `e`, `v`, `ed`, `vd`, одновременно возвращающий очищенный основной ответ; проверить корректный delta, отсутствие строки, ложный префикс внутри ответа и ошибочный JSON.
- [x] 3.2 Реализовать фазовую валидацию delta: planning принимает добавления, execution только `ed`, validation только `vd`, done ничего; проверить отклонение неизвестных ID, запрещённых ключей и всего неоднозначного обновления без частичного изменения TODO.
- [x] 3.3 Подключить извлечение delta до записи ответа в историю для обоих провайдеров и не показывать служебную строку пользователю; проверить provider-payload и agent-тестами чистый видимый ответ и чистую сохранённую историю.
- [x] 3.4 После успешного ответа атомарно сохранить допустимый delta в SQLite и обновить `ActiveMemory`, сохраняя фазу неизменной; проверить, что следующий запрос видит новые пункты/отметки, а ошибка сохранения не меняет runtime-состояние.
- [x] 3.5 При отсутствующем или некорректном delta оставить TODO неизменным, показать предупреждение и сохранить основной ответ; проверить регрессионными тестами оба сценария.

## 4. CLI и фазовый workflow

- [x] 4.1 Изменить создание задачи: исходное описание пользователя становится первым фактом, а списки наполняет planning-агент; проверить создание в фазе `planning` и первый автоматический update.
- [x] 4.2 Убрать ручное редактирование TODO из пользовательского workflow и оставить пользователю управление фазами через подтверждаемый `/task next`; проверить help, интерактивное меню и ошибочные команды.
- [x] 4.3 Перед `/task next` показывать число незавершённых пунктов текущей секции, но не запрещать подтверждённый пользователем переход; проверить переход с полным и неполным checklist, строгий порядок фаз и терминальность `done`.
- [x] 4.4 Обновить `/task show` и `/memory`: отображать факты и execution/validation-пункты как читаемые чекбоксы, сохраняя компактный TOON только для БД и prompt; проверить формат пустых, частично выполненных и завершённых списков.

## 5. Продолжение без истории и документация

- [x] 5.1 Добавить сквозной тест `planning -> execution -> validation -> done` с автоматическими отметками и ручными переходами фаз; проверить, что агент никогда не меняет фазу сам.
- [x] 5.2 Добавить сквозной тест смены/загрузки сессии на `execution` и `validation`; проверить восстановление фактов, списков, отметок и выбор следующего незавершённого пункта без истории предыдущего диалога.
- [x] 5.3 Обновить README с TOON-примером, фазовыми обязанностями агента и правилом пользовательского управления фазами; проверить соответствие списка команд фактическому CLI.
- [x] 5.4 Создать `reports/day13/report.md` с экспериментом продолжения после смены сессии и сравнением размера компактного TOON с эквивалентным JSON; проверить наличие входных данных, результатов и вывода об экономии токенов/символов.
- [x] 5.5 Запустить `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings` и `cargo test`; все команды должны завершиться успешно.

## 6. Регрессия формата ответа модели

- [x] 6.1 Принимать в последней строке совместимый формат `TASK_UPDATE {...}` и числовые ID в строках, сохраняя строгую проверку JSON, ключей и фазы; покрыть реальный execution-ответ регрессионным тестом и повторно запустить форматирование, Clippy и тесты.

## 7. Автоматический запуск новой фазы

- [x] 7.1 После подтверждённого `/task next` в `execution` или `validation` сразу отправлять агенту фазовую инструкцию через обычный request pipeline, не вызывать LLM для `done`; обновить документацию, добавить регрессионные тесты и пройти все проверки.

## 8. Детерминированный текущий шаг

- [x] 8.1 На каждом запросе `execution`/`validation` вычислять первый незавершённый пункт и явно передавать его ID, текст и ожидаемый `TASK_UPDATE`; при пустой секции запрещать повтор и предлагать `/task next`, обновить документацию и покрыть пересчёт шага тестами.

## 9. Совместимость bare JSON delta

- [x] 9.1 Распознавать bare JSON в последней строке только при наличии служебного ключа `f/e/v/ed/vd`, сохраняя обычный JSON в ответе; покрыть реальный ответ `{"ed":[3]}` и отрицательный сценарий регрессионными тестами.

## 10. Детерминированное предложение перехода

- [x] 10.1 После сохранения последнего пункта текущей фазы выводить следующую фазу и `/task next` силами CLI, не меняя фазу автоматически и не дублируя подсказку для уже завершённой секции; покрыть событие тестами и обновить документацию.

## 11. Автопродолжение внутри фазы

- [x] 11.1 После подтверждённого прогресса автоматически выполнять следующий пункт `execution`/`validation` через обычный request pipeline до завершения секции; останавливаться при отсутствии прогресса или ошибке, сохранять каждый ответ и оставить смену фазы пользователю.

## 12. Результаты задачи вне истории сессии

- [x] 12.1 Добавить связанную с задачей таблицу результатов пунктов, загрузку полного ответа и краткого итога, атомарное сохранение с отметкой TODO и защиту от перезаписи; покрыть миграцию и ошибки тестами.
- [x] 12.2 Передавать краткие итоги в рабочий prompt и полные результаты выполнения в validation, показывать результаты в `/task show` и `/memory`; проверить новую сессию и обновить документацию.
- [x] 12.3 Запустить форматирование, Clippy, тесты и строгую проверку OpenSpec.
